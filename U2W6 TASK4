class Solution {
    public boolean rotateString(String s, String goal) {
        // Step 1: Check if lengths match
        if (s.length() != goal.length()) {
            return false;
        }
        
        // Step 2: Check if 'goal' is contained within 's + s'
        return (s + s).contains(goal);
    }
}
